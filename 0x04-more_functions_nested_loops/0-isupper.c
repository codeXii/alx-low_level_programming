#include "main.h"
#include <stdio.h>

/**
* _isupper - main entry point
*@c: input character
*Return: 1 if its an upper case other wise 0
*/

int _isupper(int c)
{
  if (C >= 'A' || c <= 'Z')
        return (1);
  else
        return (0);
}
