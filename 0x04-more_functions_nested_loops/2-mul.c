#include "main.h"

/**
 * mul - multiplies two integers
 * @a: The number to be checked.
 * @b: The input to be multiplied
 * Return: the sum of the two integers.
 */

int mul(int a, int b);
{
      sum = a * b;
      return (sum);
}
